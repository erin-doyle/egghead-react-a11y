[[plugins]]
package = "netlify-plugin-a11y"

  [plugins.inputs]
  checkPaths = [
    'https://egghead-react-a11y.netlify.app/login',
  ]

  # # optional config
  # ignoreDirectories = ['/admin']  # explicitly ignore these directories

  # resultMode = "warn" # is "error" by default
